PANDA[taint2]:apply_single_label(addr: ADDRESS0, len: 4, label: CCCCCCCC)

PANDA[taint2]:apply_single_label(addr: ADDRESS4, len: 4, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS0, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS1, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS2, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS3, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS4, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS5, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS6, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS7, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: CCCCCCCC)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: DDDDDDDD)

PANDA[taint2]:query labels on memory

PANDA[taint2]:query labels on memory

PANDA[taint2]:assert positive taint_contains(addr: ADDRESS_UNLABELED, label: DDDDDDDD)

